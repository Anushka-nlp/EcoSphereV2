import 'package:flutter/material.dart';

/// ===============================================================
/// EchoSphere Design System (EDS)
/// App Colors
/// ===============================================================

class AppColors {
  AppColors._();

  // Background

  static const Color background = Color(0xFF000000);

  // Glass Surfaces

  static const Color surface = Color.fromRGBO(255, 255, 255, 0.05);

  static const Color surfaceHover = Color.fromRGBO(255, 255, 255, 0.08);

  static const Color surfacePressed = Color.fromRGBO(255, 255, 255, 0.12);

  // Borders

  static const Color border = Color.fromRGBO(255, 255, 255, 0.10);

  static const Color borderStrong = Color.fromRGBO(255, 255, 255, 0.18);

  // Divider

  static const Color divider = Color.fromRGBO(255, 255, 255, 0.08);

  // Text

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color.fromRGBO(255, 255, 255, 0.72);

  static const Color textTertiary = Color.fromRGBO(255, 255, 255, 0.50);

  static const Color textDisabled = Color.fromRGBO(255, 255, 255, 0.30);

  // Icons

  static const Color iconPrimary = Colors.white;

  static const Color iconSecondary = Color.fromRGBO(255, 255, 255, 0.70);

  static const Color iconDisabled = Color.fromRGBO(255, 255, 255, 0.30);

  // Shadows

  static const Color shadow = Color.fromRGBO(0, 0, 0, 0.40);

  static const Color ambientShadow = Color.fromRGBO(0, 0, 0, 0.08);

  // Overlay

  static const Color overlay = Color.fromRGBO(0, 0, 0, 0.60);

  // Semantic

  static const Color success = Color(0xFF22C55E);

  static const Color warning = Color(0xFFFACC15);

  static const Color error = Color(0xFFEF4444);

  static const Color info = Color(0xFF60A5FA);
}
